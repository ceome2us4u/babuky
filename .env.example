# Copy to apps/web/.env.local and fill in real values. Never commit real secrets.

# Razorpay (already have an account — pull these from the Razorpay dashboard)
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
# Same key id, exposed to the browser for Checkout.js (not the secret)
NEXT_PUBLIC_RAZORPAY_KEY_ID=

# E-signature (Documenso — self-hosted or documenso.com)
DOCUMENSO_API_URL=
DOCUMENSO_API_KEY=
